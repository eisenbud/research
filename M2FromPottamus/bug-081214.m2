restart
kk=ZZ/101
S=kk[a,b]
f = presentation module ideal(a,b)
degrees source f -- this is a list
max degrees source f -- this is a list
f = presentation module ideal(a)
degrees source f -- this is a list
max degrees source f -- this is NOT a list


